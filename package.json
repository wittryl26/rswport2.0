{
  "name": "rswport",
  "version": "1.0.0",
  "description": "RSW Portfolio application",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "http-server -p 8080",
    "serve:cors": "http-server -p 8080 --cors",
    "serve:frontend": "cd Frontend && http-server -p 8080"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "chart.js": "^4.4.0",
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "http-server": "^14.1.1"
  }
}
