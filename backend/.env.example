# Server configuration
PORT=3000

# Database configuration 
# NOTE: Make sure these credentials match your PostgreSQL installation!
DB_USER=postgres
DB_HOST=localhost
DB_NAME=portfolio_db
# IMPORTANT: Replace this with your actual PostgreSQL password
DB_PASSWORD=mypassword
DB_PORT=5432

# If you encounter "password authentication failed" errors:
# 1. Verify your PostgreSQL password 
# 2. You may need to reset your password - see db-troubleshooting.md
# 3. Consider creating a dedicated user for this application
